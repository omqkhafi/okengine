/**
 * Domain schema — abstract declarations, emitted to
 * `src/db/drizzle/` for the active dialect by `oke db` / `oke dev`.
 *
 * Add `store.schema.table` here. When the file grows, split into
 * `src/db/schema/<table>.ts` with a barrel at `src/db/schema/index.ts` and
 * delete this file so the folder becomes the declare module.
 */

export {};
