/**
 * Shorter AI models.
 *
 * Filled by `oke ai setup` / `create-oke --ai`.
 * Registry cloud: provider "openrouter" + OPENROUTER_API_KEY (no baseUrl).
 * Local self-host still needs an explicit baseUrl (or OKE_AI_URL on the binding).
 */
export {};
