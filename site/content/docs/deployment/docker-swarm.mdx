---
title: Docker Swarm
description: Deploy with docker stack deploy — docker-stack.yml, PgDog, HEALTHCHECK readiness, graceful SIGTERM, known multi-instance limits, and optional Caddy, Traefik, or nginx.
icon: Ship
source: README.md
---

Swarm consumes the stack file `oke docker --stack` writes. It adds built-in
orchestration (scale, rolling updates, routing mesh) with a lower learning
curve than Kubernetes. Scale replaceable replicas; share durable drivers.

<Callout title="The one rule">
  Deploy with `docker stack deploy`. Docker’s single `HEALTHCHECK` targets `GET /_/ready`. Treat
  every task as replaceable — shared Postgres / Redis / S3, SIGTERM via `stop_grace_period`, honest
  Signal/Channel process-local limits.
</Callout>

## Quick start

<Steps>

<Step>
### Derive the Swarm stack file

```bash
oke docker --stack
```

Emits `Dockerfile` + **`docker-stack.yml`** (overlay network, `HEALTHCHECK` →
`/_/ready`, `deploy.*`, `stop_grace_period: 30s`, resource limits from the
default **4 CPU / 8 GiB** budget — override with `--cpus` / `--memory`).

Prefer `--stack` over composing many `-c` files. The default `oke docker`
(single `docker-compose.yml`) stays for plain Compose hosts.

</Step>

<Step>
### Build the app image, init Swarm, deploy

`docker stack deploy` ignores `build` — tag first (name matches the stack file).

```bash
docker build -t oke-<app>:latest -f docker/Dockerfile .
docker swarm init
cd docker
docker stack deploy -c docker-stack.yml myapp
```

Create `secrets:` names via `docker secret create` when you use secret refs,
or keep credentials in `.env.docker` for single-node labs.

</Step>

<Step>
### Scale and roll

```bash
docker service scale myapp_app=3
docker service update --image oke-<app>:latest myapp_app
```

Prod deploy block: `update_config` (`parallelism: 1`, `order: start-first`,
`failure_action: rollback`) and `restart_policy.condition: on-failure`.

</Step>

</Steps>

## Share backing services

Pin `dev`/`prod` drivers to shared stores:

| Concern                       | Driver                       | Shared backend                                    |
| ----------------------------- | ---------------------------- | ------------------------------------------------- |
| SQL / durable journal / clock | `postgres`                   | Postgres (optionally via PgDog)                   |
| Gate rate counters            | `redis` (`drivers.store.kv`) | Redis / Valkey / Dragonfly (`images["store.kv"]`) |
| Files                         | `s3`                         | S3-compatible object store                        |
| Signal                        | see known limits below       | —                                                 |

## PgDog pooling

Bun.SQL defaults to **10** connections per process — fine for one replica.
Scale the Swarm service and `N × pool` can exceed Postgres `max_connections`.

When both `store.sql` and `pgdog` are pinned (`dev`/`prod` template default),
`DATABASE_URL` points at PgDog `:6432` — transaction pooling, wire-transparent.
Configs: `docker/pgdog/pgdog.toml` + `docker/pgdog/users.toml`.

**Why PgDog.** Transaction pooling fixes the connection math. Naive poolers can
leak session state (`SET`, RLS vars, `LISTEN`/`NOTIFY`) across clients; PgDog
re-applies those under transaction mode.

Read-replica routing (`BEGIN READ ONLY` → replica) is documented readiness —
not configured in the generated stack yet.

## Redis-protocol images (Redis · Valkey · Dragonfly)

`images["store.kv"]` defaults to Redis (most mature). Pin Valkey or Dragonfly
the same way — driver id stays `redis`, same `REDIS_URL`. Image table + one-line
licenses: [Store · KV](/docs/elements/store#kv).

## Readiness and liveness

| Endpoint       | Role                                                   | On Swarm                                              |
| -------------- | ------------------------------------------------------ | ----------------------------------------------------- |
| `GET /_/ready` | Kernel readiness (`booting` → `orphan_scan` → `ready`) | Single Docker `HEALTHCHECK` — routing mesh waits here |
| `GET /health`  | App liveness                                           | External monitors only                                |

## Graceful shutdown

`stop_grace_period: 30s` gives `installGracefulShutdown` time to release Clock /
Journal leases before the task is killed. Signal message leases reclaim after
visibility TTL on survivors.

## Reverse proxy

Same opt-in `images.proxy` pins as plain Compose — Caddy, Traefik, or nginx. Re-run
`oke docker --stack` after pinning. Details:
[Reverse proxy](/docs/deployment/reverse-proxy).

## Known multi-instance limits

Same table as [Docker](/docs/deployment/docker#known-multi-instance-limits) —
Clock / Journal / Gate redis / Store shared; Signal consume and Channel consent
stores stay process-local until durable drivers ship.

## Troubleshooting

<Accordions>
<Accordion title="stack deploy ignores build">

Build and tag `oke-<app>:latest` first (`docker build -f docker/Dockerfile .`).
Swarm never runs Compose `build:`.

</Accordion>
<Accordion title="New task gets traffic before orphan scan finishes">

Confirm `HEALTHCHECK` targets `/_/ready` in `docker-stack.yml`. Without it,
Swarm treats “container started” as ready.

</Accordion>
<Accordion title="Overlay networking / multi-node volumes">

Multi-node needs shared Postgres / Redis / S3 — do not rely on local named
volumes for durable state across nodes.

</Accordion>
</Accordions>

## Learn more

- [Docker](/docs/deployment/docker) — plain compose default
- [Reverse proxy](/docs/deployment/reverse-proxy) — Caddy / Traefik / nginx
- [Kubernetes](/docs/deployment/kubernetes) — Deployment, probes, Ingress
- [CLI](/docs/reference/cli) — `oke docker --stack`

## Next

<Cards>
  <Card title="Docker" href="/docs/deployment/docker" />
  <Card title="Reverse proxy" href="/docs/deployment/reverse-proxy" />
  <Card title="Kubernetes" href="/docs/deployment/kubernetes" />
</Cards>
