---
title: "Vault"
description: "protected knowledge — Secrets, config, and environment with typed contracts — protected knowledge, not a loose bag of env vars."
icon: "KeyRound"
source: "docs/spec/unified-theory.md"
---

Vault is the element for **protected knowledge**.

Secrets, config, and environment with typed contracts — protected knowledge, not a loose bag of env vars.

<Callout title="Governing rule">
  Secrets are write-only in the Console — set and rotate, never reveal. Flows read via `fx.vault`.
</Callout>

## At a glance

<Cards>
  <Card
    title="Vault"
    description="Essence: protected knowledge. Replaces: secrets · config · environment."
  />
</Cards>

## Drivers available

<Cards>
  <Card title=".env.local" description="Dev default — local files, no invented format." />
  <Card title="sops / age" description="Prod default — existing standard." />
</Cards>

## Example

```typescript
import { vault } from "okengine";
import { z } from "zod";

// A declaration is a CONTRACT, not a value.
// Resolution: process.env → .env.local → docker/.env.docker → vault driver → dev fallback
export const stripeKey = vault.secret("STRIPE_KEY", {
  schema: z.string().startsWith("sk_"),
  description: "Payments gateway key",
  rotate: "90d",
  dev: "sk_test_local",
});

export const dbUrl = vault.secret("DATABASE_URL", {
  schema: z.string().url(),
  dev: vault.fromDocker("store.sql"), // generated by `oke dev --docker` — zero manual setup
});
```

## Next

<Cards>
  <Card title="Channel" description="Continue to Channel." href="/docs/elements/channel" />
  <Card
    title="Introduction"
    description="Eight elements overview."
    href="/docs/get-started/introduction"
  />
  <Card
    title="Console"
    description="Panels derived from the Manifest."
    href="/docs/console/overview"
  />
</Cards>
