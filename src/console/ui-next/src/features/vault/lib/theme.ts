/**
 * Vault visual tokens — promote the existing secret hue (Traces bars).
 *
 * Cool slate, no collision with Signal amber `#FBBF24` or write orange.
 */

/** Accent for Key wells, rotation progress ink, and rewrap badges. */
export const VAULT_ACCENT = "#94A3B8";

/** Selection / progress glow (NODE_ACCENT 0.45 recipe). */
export const VAULT_GLOW = "rgba(148, 163, 184, 0.45)";

/** Icon-well fill (NODE_ACCENT 0.14 recipe). */
export const VAULT_WELL = "rgba(148, 163, 184, 0.14)";
