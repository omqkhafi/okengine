/**
 * Motion for React — ui-next animation primitives.
 * Package: `motion` (motion.dev); React entry: `motion/react`.
 */

export {
  AnimatePresence,
  animate,
  LayoutGroup,
  motion,
  useMotionValue,
  useReducedMotion,
  useTransform,
} from "motion/react";
export type { MotionValue } from "motion/react";
