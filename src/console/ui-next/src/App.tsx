/**
 * ui-next root — Phase 1 setup claim gate only.
 */

import { ClaimPage } from "./features/setup/claim-page.tsx";

/**
 * Application root for the parallel Console SPA.
 */
export function App() {
  return <ClaimPage />;
}
